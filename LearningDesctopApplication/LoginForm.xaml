<Window x:Class="LearningDesctopApplication.LoginForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Height="325" Width="320"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome Back" 
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="10">
            <Label Content="NIC:" FontWeight="Bold"/>
            <TextBox x:Name="txtNIC" Height="30" PreviewTextInput="txtNIC_PreviewTextInput"
                     Padding="5" BorderThickness="1" MaxLength="12" TextChanged="txtNIC_TextChanged"
                     BorderBrush="#ccc" Background="White"/>

            <Label Content="Password:" FontWeight="Bold" Margin="0,5,0,0"/>
            <PasswordBox x:Name="txtPassword" Height="30" 
                         Padding="5" BorderThickness="1"
                         BorderBrush="#ccc" Background="White"/>
            <Button Content="Login" Click="Login_Click"
                    Background="#2196F3" Foreground="White"
                    FontWeight="Bold" Padding="10,5"
                    Width="200" Height="35" Cursor="Hand"
                    BorderThickness="0">
            </Button>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <TextBlock Text="Don't have an account?" 
                           FontSize="12" VerticalAlignment="Center"/>
                <Button Content="Register" Click="Register_Click"
                        Foreground="#4CAF50" Background="Transparent"
                        BorderThickness="0" Cursor="Hand"
                        FontSize="12" FontWeight="Bold"
                        Padding="0,0,0,0" Margin="5,0,0,0">
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
